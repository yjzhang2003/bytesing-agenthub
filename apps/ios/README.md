# AgentHub iOS

Placeholder for the future SwiftUI client.

The iOS app will use the shared AgentHub API and event contracts from `packages/contracts`, but it will implement native SwiftUI UI rather than embedding the Web app as a primary interface.

